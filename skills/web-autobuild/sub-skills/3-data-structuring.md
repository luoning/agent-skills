# Phase 3: Data Structuring & Schema Validation

## Trigger
Use when the copy is final (Phase 2), before drafting the HTML skeleton.

---

## 1. Schema Injection Protocol
Generate high-fidelity JSON-LD data for LLM and SEO search engines. Insert the JSON-LD inside the `<head>` of the page.
*   **FAQ Sections**: Map to `FAQPage` schema.
*   **Services/Products**: Map to `LogisticsService` or `Product` schema.

---

## 2. Definition of Done (DoD)
*   [ ] The structured JSON-LD block is prepared and validated against schema standards.
*   [ ] The `.pipeline_state.json` phase 3 status is marked as `completed`.
